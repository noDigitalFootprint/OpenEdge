using OpenEdge.helper;

namespace OpenEdge.vocab;

internal class StrokingStart : BaseVocab
{
	public StrokingStart(Voc voc)
		: base(voc)
	{
		normal.AddRange(new string[3] { "start @jerkingOff", "start stroking that @cock", "begin stroking" });
		quickShot.AddRange(new string[1] { "you may start stroking, but don't you dare cum" });
		kneeling.AddRange(new string[1] { "Stay on your knees and stroke slowly" });
		degrading.AddRange(new string[2] { "start stroking again, it's pretty much all you're good for anyway", "start stroking @subTitle" });
	}
}
