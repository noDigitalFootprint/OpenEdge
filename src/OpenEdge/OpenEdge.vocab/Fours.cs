using OpenEdge.helper;

namespace OpenEdge.vocab;

internal class Fours : BaseVocab
{
	public Fours(Voc voc)
		: base(voc)
	{
		normal.AddRange(new string[3] { "I need you on all fours @pup", "get on the floor on all fours @pup", "be a good @pup and get on all fours" });
	}
}
