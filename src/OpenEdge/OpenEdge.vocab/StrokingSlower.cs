using OpenEdge.helper;

namespace OpenEdge.vocab;

internal class StrokingSlower : BaseVocab
{
	public StrokingSlower(Voc voc)
		: base(voc)
	{
		normal.AddRange(new string[2] { "slow down", "slow down a bit" });
		quickShot.AddRange(new string[3] { "you can't last long enough for a pace like this, slow down", "just a bit more and you would cum already, too bad though", "that @cock can't handle this slow down" });
		degrading.AddRange(new string[2] { "slow down @subTitle", "slow down a bit @subTitle" });
	}
}
